{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": [
    "Shared TypeScript configuration for every Maxpromo application.",
    "Applications extend this and add only what is genuinely app-specific:",
    "their own `paths` and `include`. Before this file each repository kept",
    "its own copy and the two had already drifted on target, lib and strictness."
  ],
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [{ "name": "next" }]
  },
  "exclude": ["node_modules"]
}

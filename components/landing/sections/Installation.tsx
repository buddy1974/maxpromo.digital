// TODO Phase 5: Installation section — reusable product engine component.
// Shows the system setup flow from handover to live operation.
//
// RestaurantOS  → menu import → QR setup → kitchen display → go live
// HandwerkOS    → job import → team setup → mobile app → go live
// PraxisOS      → patient import → calendar sync → portal → go live
// PrintShopOS   → file import → preflight config → queue setup → go live
// TaxKontrol    → financial import → category rules → export config → go live
//
// All variants share one section shell — product identity injected via LandingData.
export function Installation() {
  return null
}

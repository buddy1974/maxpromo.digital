/**
 * lib/registry/adapters/utils/workflow.ts
 *
 * Workflow transform helpers.
 * Currently a placeholder — workflow rendering is handled by locale.ts
 * via resolveWorkflow(). This module is reserved for future transforms
 * that operate on resolved WorkflowTuple data.
 *
 * TODO: add step numbering transform (WorkflowTuple → NumberedStep[])
 * TODO: add workflow-to-schema.org structured data transform
 * TODO: add workflow truncation helper (5 steps → 3 steps for condensed views)
 */

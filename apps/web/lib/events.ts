/**
 * lib/events.ts
 *
 * Shared custom event name constants.
 * Import from here — never use raw strings at call sites.
 */

/** Opens the Max chat panel from any surface (MobileStickyCTA, inline CTAs, etc.) */
export const OPEN_MAX_CHAT = 'open-max-chat'
